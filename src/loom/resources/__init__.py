"""Files Loom ships with, as opposed to files a site provides itself."""

from __future__ import annotations

from pathlib import Path

DEFAULT_THEME_DIR = Path(__file__).resolve().parent / "default_theme"
